{
  "name": "divitek-outlook-odoo-addin",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n UI,API -c auto \"npm -w client run dev\" \"npm -w server run dev\"",
    "build": "npm -w client run build && npm -w server run build",
    "lint": "npm -w client run lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  }
}