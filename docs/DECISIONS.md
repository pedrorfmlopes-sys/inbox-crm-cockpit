# DECISIONS

| Decisao | Estado | Motivo | Impacto | Quando rever |
| --- | --- | --- | --- | --- |
| Grupos v1: a persistencia local e reidratacao pos-apply do `Classificar` passam a correr em helper proprio | Ativa | O handler ainda concentrava inline a resolucao do storage do caso, `writeCase(...)`, sync imediato e refresh/reidratacao basica apos o apply local | `persistAndRefreshClassificationCase(...)` passa a concentrar persistencia no repositório do caso, sync imediato no studio e refresh/reidratacao pos-apply preservando a ancora/preferred key; o handler principal fica com a orquestracao e a logica Outlook | Rever quando a reidratacao Outlook/host-specific puder sair mais sem abrir promocao final |
| Grupos v1: a projecao local no `IntermediateCase` durante o apply do `Classificar` passa a correr em helper proprio | Ativa | O handler ainda concentrava inline a resolucao de `localClassificationState`, a construcao do draft local e a projecao no caso, apesar de a resolucao comum, o ticket base e a execucao remota por target ja estarem separados | `projectApplyIntoIntermediateCase(...)` passa a concentrar a construcao do draft local, a classificacao por email e a projecao canonica de anexos no caso; o handler principal fica com persist, sync, refresh e Outlook | Rever quando a persistencia local do caso puder reduzir ainda mais acoplamento sem abrir promocao final |
| Grupos v1: a execucao do ticket base no `Classificar` passa a correr em helper proprio | Ativa | O handler ainda concentrava inline a decisao de criar ticket novo e de atualizar estado do ticket existente, apesar de a resolucao comum e o plano remoto ja estarem separados | `executeLegacyBaseTicketApply(...)` passa a concentrar create/update do ticket base usando `resolvedApplySelection`, `remoteApplyPlan`, `currentContext` e `currentOutlookTicket`; o handler principal fica mais claramente como orquestrador | Rever quando a camada remota legacy puder sair ainda mais do `GroupClassificationStudioApp` sem abrir promocao final |
| Grupos v1: a execucao remota legacy por email no `Classificar` passa a correr em helper proprio por target | Ativa | Mesmo com `remoteApplyPlan`, o loop do `handleApplyClassification()` ainda concentrava demasiadas chamadas remotas por email e escondia a ordem operacional no meio da orquestracao | `executeLegacyRemoteApplyForTarget(...)` passa a concentrar remove/add de grupos, registo do email relevante e unlink/link de tickets por target; o handler principal fica responsavel por resolucao comum, ticket base, projecao no `IntermediateCase`, refresh e Outlook | Rever quando a camada remota legacy puder sair ainda mais do `GroupClassificationStudioApp` sem abrir promocao final |
| Grupos v1: a execucao remota legacy do apply no `Classificar` passa a correr sobre um plano remoto explicito | Ativa | Mesmo com `resolvedApplySelection`, a ordem remota e os side effects legacy ainda estavam demasiado misturados no `handleApplyClassification()` | `remoteApplyPlan` passa a concentrar targets por email, payload base/classificado, grupos a remover, ticketIds a desligar e contexto do email atual para Outlook; a execucao continua legacy e no client, mas fica melhor separada da resolucao comum e da projecao no `IntermediateCase` | Rever quando a camada remota legacy puder ser extraida mais sem abrir promocao final |
| Grupos v1: o apply local do `Classificar` passa a resolver primeiro uma base comum antes de alimentar UI, legado remoto e `IntermediateCase` | Ativa | O `handleApplyClassification()` ainda recalculava targets, payloads classificados e draft do caso em varios pontos, aumentando o risco de divergencia entre o que a UI mostrava e o que era gravado localmente | `resolvedApplySelection` passa a concentrar targets por scope, grupo principal, referencias, labels, ticket e metadados efetivos; helpers puros constroem o payload base por email, o payload classificado e o draft para o `IntermediateCase`; chamadas remotas legacy continuam separadas como camada de execucao | Rever quando o pipeline legacy remoto puder reduzir mais sem abrir promocao final |
| Grupos v1: no bloco de anexos/documentos do `Classificar`, o estado canonico por `email + anexo` manda antes do estado editorial de preview e quick docs | Ativa | O studio ainda usava chaves de anexo demasiado globais e misturava demasiado a colecao canonica do caso com preview, quick docs e filtros locais | A base de anexos passa a nascer primeiro do email canonico do `IntermediateCase`; quick docs, preview e picker usam chave composta `emailKey + attachmentKey`; `documentState` e `isHidden` continuam canonicos, enquanto preview, expand/collapse, filtro de silenciados e `attachmentPlan` ficam explicitamente editoriais | Rever quando o bloco de anexos puder reduzir mais estado local sem abrir promocao final |
| Grupos v1: no bloco de grupos/referencias do `Classificar`, a base canonica do email manda antes da pesquisa editorial | Ativa | O studio ainda deixava `principalSearch`, `referenceSearch` e algum estado bruto do editor funcionarem demasiado como espelho do grupo principal/referencias aplicados | `effectivePrincipalGroupId` e `effectiveReferenceGroupIds` passam a sustentar resumos, chips ativos, favoritos, apply e reidratacao; a pesquisa continua local/editorial e deixa de contar como alteracao funcional pendente ou como reflexo implicito do estado aplicado | Rever quando os restantes drafts editoriais do bloco de grupos puderem reduzir mais sem abrir promocao final |
| Grupos v1: no bloco de tickets do `Classificar`, o ticket canonico do email manda antes da pesquisa editorial | Ativa | O studio ainda misturava demais tickets contextuais/canonicos com resultados transitarios de pesquisa, o que deixava ambigua a resolucao do ticket real do email | `canonicalTicketChoices` passa a representar a base canonica/contextual; `ticketPickerChoices` fica apenas como lista editorial do picker; `selectedTicket` resolve primeiro o ticket do caso/email e so cai em pesquisa quando o utilizador esta mesmo a editar manualmente | Rever quando o editor de tickets puder reduzir mais drafts locais sem abrir promocao final |
| Grupos v1: no `Classificar`, o que ja for canonicamente derivavel do `IntermediateCase` deixa de depender de estado paralelo editorial | Ativa | O studio ainda mantinha demasiada coerencia basica em estados locais como seed parcial por email e `ticketSearch`, mesmo quando o caso canonico ja tinha dados suficientes | A mudanca de email passa a reidratar explicitamente grupo principal, referencias, labels e `ticketId` a partir do caso; `ticketSearch` continua apenas como ferramenta editorial e deixa de contar como alteracao funcional pendente | Rever quando o resto dos drafts editoriais puder ser reduzido sem abrir promocao final |
| Grupos v1: apos apply local no `Classificar`, o `IntermediateCase` atualizado volta a mandar imediatamente no estado interno do studio | Ativa | O caso canonico ja era atualizado no apply, mas o pos-apply ainda ficava demasiado dependente de refreshes legacy e estados paralelos stale | O studio passa a ressemear o email selecionado, etiquetas canonicas, grupo/referencias, `ticketId` e listas case-backed a partir do `IntermediateCase` logo apos o `writeCase(...)` e novamente apos o refresh legacy; o fallback antigo continua apenas como complemento seguro | Rever quando a dependencia legacy pos-apply puder ser reduzida outra vez sem abrir promocao final |
| Grupos v1: a classificacao local de etiquetas no `IntermediateCase` passa a guardar o estado final por email necessario para reabertura fiel do studio | Ativa | O caso canonico ainda nao guardava com fidelidade suficiente as etiquetas, removidas herdadas, estados por etiqueta e categorizadas, obrigando a heuristicas ou legado na reabertura | O `IntermediateCase` passa a guardar por email `labels` finais ativas, `removedInheritedLabels`, `labelStates` e `categorizedLabelNames`; o `Classificar` reabre estes campos diretamente do caso e o legado fica apenas como fallback para cenarios antigos | Rever quando o resto do editor rico de etiquetas puder ser canonizado sem abrir promocao final |
| Grupos v1: a classificacao local do `Classificar` passa a escrever por email no `IntermediateCase` | Ativa | O studio ja abria do caso canonico, mas ainda deixava a classificacao local sobretudo em estados paralelos da UI | O apply passa a projetar grupo principal, referencias, labels, tickets conhecidos, `state`/`status` e estados de anexos no `IntermediateCase`, por email alvo e por scope; drafts ricos como `labelDrafts`, `classificationMetaDraft`, `selectedSeriesId`, `ticketStatusDraft` e `attachmentPlan` continuam locais nesta fase | Rever quando o editor rico puder reduzir ainda mais estados paralelos e antes da promocao final |
| Grupos v1: no `Classificar`, o `IntermediateCase` passa a ser a base interna principal sempre que existir | Ativa | O caso canonico ja chegava ao studio, mas o interior ainda dependia demasiado de listas legacy e reconstrucao vaga | `classificationCase`, ancora, conjunto contextual e total conhecido passam a nascer primeiro do `IntermediateCase`; `seedKey`, `prepareSeedKey`, leituras de servidor e listas legacy ficam como fallback/enriquecimento para cenarios sem caso canonico ou com dados parciais | Rever quando a dependencia legacy interna puder ser reduzida outra vez sem risco e antes da promocao final |
| ChatGPT atua como gestor tecnico/orientador | Ativa | Garantir continuidade estrategica, priorizacao e coerencia entre rondas | A direcao tecnica nao depende do contexto parcial de um executor isolado | Rever se o modelo de coordenacao mudar |
| Agents externos trabalham por turno | Ativa | Reduzir conflito de contexto e tornar cada intervencao auditavel | Cada ronda deve ter ambito claro, validacao propria e handoff explicito | Rever se houver workflow multi-agent concorrente formalizado |
| Git e a fonte de verdade | Ativa | Conversas e memorias antigas podem divergir do estado real do projeto | Toda a analise e implementacao deve partir do repo e nao de contexto informal | Nunca, salvo mudanca total de processo |
| Antes de implementar, ler `AGENTS.md`, `docs/HANDOFF.md` e `docs/DECISIONS.md` | Ativa | Forcar alinhamento minimo entre agents | Reduz regressões por contexto incompleto | Rever apenas se estes ficheiros forem substituidos por outro mecanismo canonico |
| Cada ronda relevante deve atualizar o handoff | Ativa | Evitar perda de contexto operacional entre turnos | O estado do projeto fica documentado no proprio repo | Rever se surgir outra pratica equivalente e melhor |
| Seguranca e arquitetura vem antes de features | Ativa | O diagnostico atual mostra maior risco estrutural do que falta funcional | Prioriza estabilizacao antes de expansao | Rever quando a base tecnica estiver consolidada |
| Nunca usar contexto antigo como verdade sem validar no repo | Ativa | Ha historico longo e risco real de informacao desatualizada | Obriga a distinguir facto, hipotese e memoria | Nunca |
| Qualquer hipotese sobre producao deve ser marcada como hipotese, nao como facto | Ativa | O repositorio nao prova sozinho a configuracao real de Render/Supabase/manifest | Melhora rigor tecnico e evita decisoes erradas | Rever se passar a existir acesso direto e estavel aos ambientes |
| Evitar regressões e mudancas transversais nao delimitadas | Ativa | O projeto tem ficheiros sensiveis, monoliticos e acoplamento elevado | Cada alteracao deve ser cirurgica, com raio de impacto controlado | Nunca |
| Nao mexer em zonas sensiveis sem necessidade explicita | Ativa | `office.ts`, `CockpitProvider`, `linkStore`, `index.js`, Odoo e manifest concentram risco alto | Aumenta prudencia em areas que podem quebrar multiplos fluxos | Rever quando essas zonas forem consolidadas |
| Confirmar primeiro a verdade arquitetural antes de otimizacoes profundas | Provisoria | Ainda ha duvidas relevantes sobre producao, Postgres/Supabase e fontes de verdade | Evita otimizar ou refatorar com premissas erradas | Rever apos validacao de producao |
| Alteracoes documentais tambem devem refletir a estrategia atual do projeto | Ativa | A documentacao passa a ser parte do workflow operacional | Handoff e checklist tornam-se instrumentos de trabalho, nao anexos decorativos | Rever se o processo documental deixar de ser canonico |
| Nao armazenar segredos (passwords, API keys) em settings persistidas no cliente | Ativa | Risco critico de exposicao e compromisso de credenciais | Segredos devem ser geridos via Vault/Server e o cliente usa apenas tokens de sessao | Revisar apos implementacao de Vaulting |
| Centralizacao de Estados e Cores (Nao Destrutiva) | Ativa | Garantir uniformidade visual e semantica sem o risco de migracao de dados em ambiente real | `statusUtils.ts` mapeia aliases para 4 cores canonicas; backend preserva o valor literal | Revisar se houver necessidade de limpeza ou migracao massiva de BD |
| UI de Elevada Densidade: Cards Compactos de Linha Unica | Ativa | O espaco de visualizacao na sidebar dita o workflow de suporte | Todos os cards contextuais usam flex de 1 linha sem alturas variaveis | Revisar se houver redesign total da UX ou novos form-factors |
| Limitacao Add-In API: Outlook Category Projections | Ativa | Office.js apenas suporta acoes ativas robustas no item em leitura corrente | Scopes como "Todos emails do caso" recebem persistence no Odoo, mas a API do painel lateral apenas garante flush `categories.addAsync` para o item em ecra | Revisar caso Office.js permita batch update de itens nao em reading pane, ou usar Graph API webhook syncs |
| Refactoring Estrutural: separacao de logica apatrida em modulos CRM especializados | Ativa | Reduzir a carga cognitiva e o acoplamento no `GroupClassificationStudioApp.tsx`, facilitando manutencao e testes unitarios de helpers puros | Extracao de tipos, constantes e documentUtils para `client/src/modules/crm/group-classification/`. O Studio importa estes helpers sem alterar comportamento funcional | Rever se o modulo se tornar demasiado grande ou exigir submodulos |
| Grupos v1: baseline canonica = plano + mockups/exportacoes mais recentes referenciados em `docs/grupos_v1_index.md` | Ativa | Evitar ambiguidade entre relatorios historicos e baseline operacional | Novas rondas de Grupos v1 devem partir primeiro do plano e dos mockups/exportacoes de 2026-04-09 | Rever quando existir baseline mais recente aprovada |
| Grupos v1: `Explorar`, `Explorador de Grupos` e `Gestor do Grupo` sao superficies distintas | Ativa | Proteger responsabilidades e evitar mistura conceptual | `Explorar` e consulta rapida global no add-in; `Explorador` e consulta aprofundada; `Gestor` e o editor rico unico | Rever apenas com redesign aprovado |
| Grupos v1: o Explorador consulta e abre o Gestor; nao duplica edicao rica | Ativa | O mockup intermedio do Explorador pode induzir um segundo editor por engano | Mantem um unico editor completo e reduz risco de divergencia funcional | Rever apenas com novo mockup aprovado |
| Grupos v1: progresso de sessao e persistencia remota sao camadas separadas | Ativa | Reduzir escrita prematura e proteger trabalho em curso | Sessao local retem progresso; promocao remota acontece antes de sair do contexto ou por save explicito | Rever quando a estrategia final de storage estiver estabilizada |
| Grupos v1: a tab `Groups` do task pane representa `Preparar`; o `GroupManagerCockpit` permanece reservado ao Gestor | Ativa | Evitar colapsar a entrada compacta do add-in com o editor rico unico | A sidebar usa uma superficie leve de preparacao; o Gestor continua separado para edicao rica e vista dedicada | Rever apenas com redesign aprovado |
| Grupos v1: Fase 2 usa sessao local e seed minimo para `Classificar`, sem persistencia remota pesada | Ativa | Fechar a entrada `Preparar` com baixo risco e sem antecipar a Fase 3 | O estado transitorio vive em storage local e a integracao final com `Classificar` continua faseada | Rever ao implementar Fase 3/4 |
| Grupos v1: `prepareSession` continua estritamente limitado a progresso local de `Preparar` | Ativa | Evitar que a cache de sessao derive para base de verdade paralela | A sessao pode guardar apenas selecao, toggles, filtros, grupo em trabalho, anexos preparados e metadata local de save; nao guarda payload final nem verdade remota | Rever apenas se existir arquitetura nova aprovada para persistencia local |
| Grupos v1: save before exit/context change faz flush local, nao promocao remota | Ativa | Proteger trabalho em curso sem antecipar a Fase 4 nem abrir backend pesado | `Preparar` grava em `sessionStorage` antes de mudar de sub-vista, email/contexto, sair da superficie ou abrir `Classificar`; `localStorage` fica reservado apenas a seeds temporarios com TTL | Rever ao implementar Fase 4 |
| Grupos v1: a ponte `Preparar -> Classificar` e unidirecional, temporaria e descartavel | Ativa | Evitar que o seed de bootstrap derive para store paralela ou sincronizacao escondida | `Preparar` escreve um seed temporario; `Classificar` le, valida TTL/conteudo, consome uma vez e limpa. Sem sync bidirecional, sem verdade canonica e sem backend novo nesta fase | Rever apenas com arquitetura posterior de persistencia/promocao aprovada |
| Grupos v1: storage canonico separa sessao, persistencia principal e promocao remota | Ativa | Fechar a arquitetura de dados e reduzir egress prematuro | `sessionStorage` e apenas rascunho; a base principal depende do modo ativo; Supabase recebe apenas o que for promovido por politica | Rever quando existir implementacao funcional completa do workset persistente |
| Grupos v1: anexos grandes nao sobem automaticamente e payloads pobres nao apagam payloads bons | Ativa | Proteger qualidade dos dados e custos de storage/egress | A politica de anexos passa a ter limiar configuravel, promocao binaria automatica desligada por defeito e gate de overwrite conservador | Rever quando houver telemetria real do fluxo de storage |
| Grupos v1: a primeira persistencia principal funcional de worksets arranca por `supabase` e `hybrid`, com manifesto pequeno e metadata first | Ativa | Tirar `Preparar` da dependencia exclusiva da sessao sem fingir que os destinos locais ja estao fechados | `supabase` e `hybrid` passam a salvar e reler manifesto persistido; `local_device` e `chosen_folder` mantem-se parciais; anexos continuam sem promocao binaria automatica | Rever quando a escrita principal local estiver completa |
| Grupos v1: storage/worksets nao importam o hub global `client/src/api.ts` | Ativa | Evitar ciclos de arranque/TDZ no bundle do add-in Outlook | A fronteira HTTP de worksets fica em `groups-v1/storage/worksetApi.ts`; `saveWorkset.ts` e `loadWorkset.ts` dependem dessa folha fina, nao de `api.ts` | Rever apenas se existir cliente HTTP partilhado sem dependencias de runtime pesado |
| Grupos v1: `cockpitSettingsV1` guarda apenas settings pequenas e conhecidas | Ativa | Evitar quota errors e impedir que worksets, caches, emails ou anexos fiquem presos em settings locais/roaming | `settings.ts` filtra chaves desconhecidas antes de serializar; sessao fica em `prepareSession`; worksets ficam na persistencia principal; settings mantem apenas preferencias, limites e paths pequenos | Rever se for criado um novo contrato versionado de settings |
| Grupos v1: `Preparar` mostra estados de localizacao simples (`Rascunho`, `Local`, `Servidor`) | Ativa | `Hibrido` e modo tecnico interno, nao uma etiqueta compreensivel no card; cards fechados precisam de maxima limpeza no task pane | O email ancora aparece apenas no bloco `Email ancora`; cards fechados mostram so assunto, remetente e data/hora; estados detalhados e semantica grupo/referencia/ticket/anexos ficam no expandido | Rever apenas se houver novo mockup aprovado para a Lista de `Preparar` |
| Grupos v1: workset persistido nao equivale a email em `Servidor` | Ativa | O manifesto de workset e infraestrutura de retoma/progresso, nao prova que a classificacao principal do email esteja finalizada no Supabase | `Servidor` so pode aparecer quando o payload do email traz sinais funcionais persistidos de classificacao/ligacao; workset ou sessao salvos contam no maximo como `Local` | Rever quando existir modelo final de promocao remota por email/classificacao |
| Grupos v1: estado e aviso do email atual nao herdam historico relacionado | Ativa | Emails antigos da mesma conversa podem ter grupos reais sem que o email atual esteja classificado | `Servidor` e aviso de mudanca de grupo em `Preparar` usam apenas sinais proprios do email atual; contexto relacionado continua auxiliar e nao contamina a semantica do ancora | Rever quando existir modelo final por email/classificacao com provas remotas explicitas |
| Grupos v1: `Preparar` nao faz escrita remota automatica ao abrir/preparar | Ativa | Abrir email ou montar workset e infraestrutura de trabalho, nao classificacao final/promocao remota | Quando a tab ativa e `groups`, o cockpit provider nao chama `registerRelevantEmail`; a vista `Preparar` tambem nao regista email por falta de payload nem faz flush remoto de workset em `supabase`/`hybrid`; promocao ao servidor fica reservada a classificacao final ou acao explicita futura | Rever quando existir gate final aprovado para promocao remota por email/classificacao |
| Grupos v1: payload relacionado separa email atual de historico | Ativa | O servidor nao deve obrigar o client a adivinhar se um grupo pertence ao email atual ou a outros emails da conversa | `/api/links/related` devolve `email` apenas com match direto forte; historico de conversa e sugestoes ficam em `emails`/`groups`; `Preparar` e `Classificar` nao tratam contexto historico como grupo real do email atual | Rever se existir um contrato versionado de contexto relacionado |
| Grupos v1: abrir `Classificar` e read-only ate apply final | Ativa | Carregar contexto ou consumir seed de `Preparar` nao e classificacao final e nao deve criar persistencia remota prematura | `GroupClassificationStudioApp` nao chama `registerRelevantEmail` durante o arranque; persistencia de email/grupo/ticket fica concentrada no fluxo explicito de gravar/aplicar classificacao | Rever quando existir outro gate explicito de promocao remota aprovado |
| Grupos v1: `Filtros` OFF em `Preparar` significa zero filtragem | Ativa | Evitar filtros escondidos que tornam lista e aba `Anexos` incoerentes para o utilizador | Pesquisa, modo de anexos e modo de grupo so filtram quando o painel `Filtros` esta ligado; a lista de relacionados remove apenas o ancora por identidade direta | Rever apenas com novo contrato aprovado para `Preparar` |
| Grupos v1: Settings da aba `Groups` avancam primeiro como shell visual compacta | Ativa | Fechar a janela de configuracao sem abrir cedo demais a logica profunda de storage, migracao, manutencao ou servidor | O icon de engrenagem abre um modal leve com menu lateral e uma secao ativa de cada vez; campos, toggles, tooltips e acoes ficam preparados, mas a ligacao funcional pesada entra em rondas proprias | Rever quando a logica real de settings de Grupos for ligada |
| Grupos v1: a shell de Settings persiste num bloco proprio `groupsTabSettings` dentro de `cockpitSettingsV1` | Ativa | Ligar a janela de Settings a persistencia real da app sem abrir storage real, servidor ou fluxos profundos de negocio | A shell abre com valores reais, edita draft local, guarda via `saveSettings` e reabre com os valores persistidos; paths continuam configuracao leve e as acoes pesadas mantem-se stub | Rever quando a ligacao funcional real a storage/migracao/manutencao for implementada |
| Grupos v1: `groupsTabEnabled` e `storageMode` passam primeiro por bloqueios locais leves na aba `Groups` | Ativa | Dar efeito real aos settings sem abrir o refactor pesado nem fingir storage real | `groupsTabEnabled = false` bloqueia localmente o uso da aba com estado claro e Settings acessiveis; `storageMode = disabled` impede o fluxo de `Preparar` de agir como se a base intermedia estivesse ativa; `locationStatus`, `baseFolderPath` e `quickDiagnostic` aparecem como resumo leve do estado configurado | Rever quando a base intermedia real e a validacao de localizacao forem implementadas |
| Grupos v1: `groupsTabSettings` e a fonte principal do comportamento leve/local de `Preparar`; `groupStorage` fica apenas como legado tecnico de storage | Ativa | Evitar que a UI/local gating dependa do bloco antigo de storage e reduzir ambiguidade entre configuracao funcional e runtime tecnico | `GroupsPrepareCockpit` le gating, estado e mensagens locais a partir de `groupsTabSettings`; `groupStorage` permanece apenas encapsulado em `legacyStorageRuntime` para workset/storage e politica tecnica de anexos inline, ate existir a ronda de storage real | Rever quando a politica real de anexos e a base intermedia real forem implementadas |
| Grupos v1: a base intermedia canonica passa a ser modelada por caso, com separacao explicita entre caso, email e anexo | Ativa | O fluxo futuro `Servidor -> Intermedio -> Outlook -> Classificar -> Servidor -> Limpar intermedio` precisa de um contrato estavel e reabrivel, sem depender de reconstrucoes vagas | O caso intermedio passa a viver em `case.json` por `caseId`, com emails relacionados, classificacao por email, anexos por email, resumos de mixed case e referencias fisicas alvo para anexos; worksets antigos continuam apenas como compatibilidade/transicao | Rever quando a integracao real com a base intermedia substituir o workset atual |
| Grupos v1: apagar um caso intermedio implica remover a arvore completa do caso | Ativa | Apagar apenas `case.json` deixaria anexos orfaos e quebraria a semantica do repositorio canonico da base intermedia | `deleteCase(caseId)` passa a operar sobre `Groups/cases/<caseId>/...` inteiro; adapters de storage devem suportar remocao da arvore completa, nao so do manifesto | Rever quando existir implementacao real de filesystem/OneDrive/SharePoint |
| Grupos v1: `Preparar` passa a usar `IntermediateCase` como estrutura principal de caso/email/anexo; workset fica apenas como ponte de draft | Ativa | O cockpit precisava de deixar de trabalhar so com fragmentos soltos antes de abrir storage real ou reescrever `Classificar` | `GroupsPrepareCockpit` monta e atualiza um `IntermediateCase` a partir do email atual e relacionados; a lista de emails e anexos ativos nasce do caso canonico; o workset legado continua apenas para selecao/filtros/grupo em trabalho e seed temporaria ate a ronda de storage real | Rever quando `Preparar` e `Classificar` forem totalmente reescritos sobre a base intermedia real |
| Grupos v1: filtros de `Preparar` afetam apenas a vista, nunca o `IntermediateCase` canonico | Ativa | O caso canonico nao pode perder emails so porque a UI esconde temporariamente parte do conjunto | `IntermediateCase` passa a ser montado a partir do conjunto base completo e preserva emails ja integrados; `visibleEmails` e `visibleListEmails` tornam-se apenas projecoes filtradas da UI | Rever quando existir politica explicita de remocao de emails do caso |
| Grupos v1: a primeira camada de storage intermédio real do `IntermediateCase` usa IndexedDB do host; `baseFolderPath` ainda nao e uma pasta real utilizavel diretamente | Ativa | O host/repo atual mostra suporte real para IndexedDB, mas ainda nao existe bridge segura para escrever na localizacao configurada pelo utilizador nem OneDrive/SharePoint reais | `case.json`, listagem e procura por email passam a persistir em IndexedDB namespaced por `baseFolderPath`; blobs de anexos so sao escritos quando o conteudo ja existe em memoria; a localizacao configurada continua sem validacao/escrita real nesta fase | Rever quando existir bridge real para filesystem/OneDrive/SharePoint |
| Grupos v1: apos integrar #16-#18 em `main`, o proximo gate e teste real no Outlook | Ativa | A stack corrige arranque/worksets, settings/search e limpeza visual de `Preparar`; novas fases devem esperar validacao em host real | Nao abrir `Explorar`, `Explorador`, `Gestor` ou novas frentes ate validar arranque, settings, pesquisa, lista sem ancora duplicado e leitura no Outlook real | Rever apos teste real do utilizador |
| IA: assinatura oficial vive em `cockpitSettingsV1` | Ativa | Evitar duas fontes paralelas (`cockpitSettingsV1` e `icc.sig.*`) para a mesma assinatura | O modulo IA usa settings oficiais para prompt/output; `icc.sig.*` fica apenas como legado migravel best-effort e deixa de ser escrito como fonte funcional | Rever se existir contrato versionado novo de assinatura |
| IA: `Dirigir resposta a` e instrucao de escrita, nao destinatario Outlook | Ativa | Em cadeias de reencaminhamento, a pessoa-alvo do texto pode ser diferente do ultimo remetente e diferente de To/Cc/Bcc | `replyDirection` orienta o prompt para o interlocutor principal indicado, sem auto-preencher To, sem procurar emails e sem alterar destinatarios reais | Rever apenas se houver decisao explicita de automatizar destinatarios |
| IA: `AiCockpit` e a superficie principal; `AiPanel` e legado/secundario | Ativa | O shell atual monta `client/src/modules/ai/AiCockpit.tsx`; manter duas superficies com promessa funcional igual aumentaria divergencia e duplicacao | Novas capacidades completas de IA sao fechadas no `AiCockpit`; `AiPanel` conserva compatibilidade basica e nao deve ser vendido como fluxo principal sem consolidacao explicita | Rever se `AiPanel` for removido ou reintegrado como superficie ativa |
| IA: MODS vivem apenas em `cockpitSettingsV1.responsePresets` | Ativa | O editor antigo do `AiPanel` gravava `crmCockpit.templates.v1`, criando uma segunda fonte para a mesma feature | AI Settings e o editor oficial; `AiCockpit` consome apenas `responsePresets`; `AiPanel` legado deixa de editar/gravar templates proprios e apenas consome os MODS oficiais; `crmCockpit.templates.v1` e migrado uma vez quando elegivel e depois descontinuado | Rever apenas se existir novo contrato versionado de presets |
| Grupos v1: a UI local de `Preparar` deve seguir o resolver real de storage intermÃ©dio, nao apenas `storageMode` | Ativa | `storageMode = onedrive_sharepoint` sem `baseFolderPath` configurada nao equivale a persistencia real pronta | `GroupsPrepareCockpit` usa `intermediateCaseStorage.availability` para distinguir `ready`, `missing_location` e `disabled`; `missing_location` fica explicitamente como modo transitorio em memoria, com aviso claro e sem se apresentar como storage real pronto | Rever quando existir bridge real para a localizacao do utilizador |
| Grupos v1: a abertura do caso em `Preparar` segue a precedencia `Servidor -> Intermedio -> Outlook` | Ativa | O cockpit precisava de uma regra explicita para escolher a melhor fonte real disponivel sem deixar o historico contaminar o email atual | `getRelatedEmailContext(...)` e a leitura de servidor desta frente; `readCase(...)` / `findCaseByEmailKey(...)` sao a camada intermedia; o contexto do email aberto no host e o fallback Outlook; o `IntermediateCase` final guarda `primarySource` e `sourceOrigin` por email | Rever quando existir promocao real para servidor e reescrita profunda de `Classificar` |

| Grupos v1: o handoff `Preparar -> Classificar` passa a usar o `IntermediateCase` como fonte principal | Ativa | O seed legacy em `localStorage` era fragil como verdade principal e deixava `Classificar` dependente de reconstrucao parcial da vista | `Preparar` persiste o `IntermediateCase` antes de abrir `Classificar` e envia `caseId` + `anchorEmailKey`; `Classificar` tenta `readCase(caseId)` / `findCaseByEmailKey(anchorEmailKey)` antes de cair para `seedKey` / `prepareSeedKey`, que ficam apenas como ponte temporaria | Rever quando o fallback legacy puder ser removido sem partir cenarios antigos |
